  @Test
    public void testPrivateMethod() throws Exception {
        // Örnek bir yapılandırma
        ReverseProvisionFactory factory = new ReverseProvisionFactory(/* gerekli bağımlılıklar */);

        // Private metoda erişim için Reflection kullanma
        Method privateMethod = ReverseProvisionFactory.class.getDeclaredMethod("yourPrivateMethodName", /* parametre türleri */);
        privateMethod.setAccessible(true);

        // Metodu çağırma
        Object result = privateMethod.invoke(factory, /* parametreler */);

        // Sonuçları doğrulama
        assertEquals(expectedResult, result);
    }
