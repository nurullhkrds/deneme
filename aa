  rowSelection={{
        ...rowSelection, // rowSelection ayarlarını doğru şekilde kullanın
        selectedRowKeys: selectedRowKeys, // selectedRowKeys'ı ekleyin
        onChange: (keys, rows) => {
          setSelectedRowKeys(keys);
          console.log('Row selection changed:', keys, rows);
        },
      }}
